# Stripe Fetch Example

```sh
npm install
dataland deploy
```

This module replicates the following objects from Stripe into Dataland:

- `customer`
- `subscription`
- `invoice`
- `paymentIntent`
- `refund`
