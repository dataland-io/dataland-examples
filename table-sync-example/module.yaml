moduleId: table-sync-example-example

buildCommand: npm run build

workers:
  - workerId: table-sync-example
    scriptPath: dist/tableSyncExample.bundle.js
    triggers:
      transaction:
        enabled: true
